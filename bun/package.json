{
  "name": "bun",
  "type": "module",
  "scripts": {
    "build": "docker build --tag benchmark:bun .",
    "run": "echo 1 | bash run.sh",
    "run:multiple:times":  "echo 100 | bash run.sh",
    "build:and:run": "npm run build && npm run run",
    "test": "echo \"$PWD\" "
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}